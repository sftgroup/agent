# QA_REVIEW_REPORT — L1+L2 功能审查报告

> **模板版本**: v1.0 | **使用 Agent**: qa | **模型**: DeepSeek V4 Pro

---

## 📋 基本信息

| 字段 | 值 |
|---|---|
| 项目名称 | `{PROJECT_NAME}` |
| 审查日期 | `{YYYY-MM-DD}` |
| Commit Hash | `{COMMIT_HASH}` |
| 审查范围 | `{FILES_PATHS}` |
| PRD 文档 | `{PRD_PATH}` |
| 报告路径 | `$AGENT_WORKSPACE/test-reports/QA_REVIEW_REPORT.md` |

---

## 1. L1 表面审查

### 1.1 代码格式 & 风格

| # | 文件 | 行号 | 问题 | 严重度 | 建议 |
|---|---|---|---|---|---|
| L1-01 | `{file}` | `{line}` | `{description}` | 🟢 Low | `{suggestion}` |

### 1.2 命名规范

| # | 文件 | 行号 | 当前命名 | 问题 | 严重度 | 建议命名 |
|---|---|---|---|---|---|---|
| L1-02 | `{file}` | `{line}` | `{current}` | `{issue}` | 🟢 Low | `{suggested}` |

### 1.3 注释 & 文档

| # | 文件 | 行号 | 问题 | 严重度 | 建议 |
|---|---|---|---|---|---|
| L1-03 | `{file}` | `{line}` | `{missing/outdated}` | 🟡 Medium | `{suggestion}` |

### 1.4 一致性检查

| # | 范围 | 不一致项 | 严重度 | 建议 |
|---|---|---|---|---|
| L1-04 | `{scope}` | `{inconsistency}` | 🟡 Medium | `{unified_approach}` |

---

## 2. L2 逻辑审查

### 2.1 边界条件

| # | 文件 | 函数 | 边界问题 | 严重度 | 复现步骤 | 修复建议 |
|---|---|---|---|---|---|---|
| L2-01 | `{file}` | `{fn}()` | `{edge_case}` | `{severity}` | `{steps}` | `{fix}` |

### 2.2 错误处理

| # | 文件 | 函数 | 错误场景 | 当前处理 | 问题 | 严重度 | 建议 |
|---|---|---|---|---|---|---|---|
| L2-02 | `{file}` | `{fn}()` | `{error}` | `{current}` | `{issue}` | `{severity}` | `{fix}` |

### 2.3 空值/Null 检查

| # | 文件 | 行号 | 变量 | 缺失检查 | 严重度 | 影响 |
|---|---|---|---|---|---|---|
| L2-03 | `{file}` | `{line}` | `{var}` | `{missing_check}` | `{severity}` | `{impact}` |

### 2.4 输入校验

| # | 文件 | 函数 | 参数 | 校验缺失 | 严重度 | 攻击面 | 建议 |
|---|---|---|---|---|---|---|---|
| L2-04 | `{file}` | `{fn}()` | `{param}` | `{missing_validation}` | `{severity}` | `{attack_surface}` | `{fix}` |

### 2.5 返回类型 & 状态码

| # | 文件 | 函数 | 问题 | 严重度 | 建议 |
|---|---|---|---|---|---|
| L2-05 | `{file}` | `{fn}()` | `{return_issue}` | `{severity}` | `{fix}` |

### 2.6 并发/竞态 (中心化项目)

| # | 文件 | 函数 | 并发问题 | 严重度 | 复现条件 | 建议 |
|---|---|---|---|---|---|---|
| L2-06 | `{file}` | `{fn}()` | `{race_condition}` | `{severity}` | `{condition}` | `{fix}` |

---

## 3. 功能完整性对照表

对照 PRD 逐项验证：

| # | PRD 功能点 | 实现状态 | 文件 | 偏差说明 |
|---|---|---|---|---|
| F-01 | `{feature}` | ✅/⚠️/❌ | `{file}` | `{deviation}` |

---

## 4. Bug 诊断 (如果有)

| # | Bug ID | 严重度 | 症状 | 根因文件 | 根因分析 | 影响范围 | 复现步骤 |
|---|---|---|---|---|---|---|---|
| B-01 | `{bug_id}` | `{severity}` | `{symptom}` | `{root_file}` | `{root_cause}` | `{impact}` | `{steps}` |

---

## 5. 测试覆盖评估

### 5.1 合约项目

| 测试文件 | 测评函数数 | 覆盖行% | 覆盖分支% | 关键路径缺失 |
|---|---|---|---|---|
| `{test_file}` | `{count}` | `{line_pct}` | `{branch_pct}` | `{missing_critical}` |

### 5.2 中心化项目

| 测试文件 | 测评函数数 | 覆盖行% | 边界测试 | 异常路径测试 |
|---|---|---|---|---|
| `{test_file}` | `{count}` | `{line_pct}` | `{edge_count}` | `{exception_count}` |

---

## 6. 统计汇总

| 严重度 | L1 表面 | L2 逻辑 | 功能缺失 | Bug | 总计 |
|---|---|---|---|---|---|
| 🔴 Critical | `{n}` | `{n}` | `{n}` | `{n}` | **{sum}** |
| 🟠 High | `{n}` | `{n}` | — | `{n}` | **{sum}** |
| 🟡 Medium | `{n}` | `{n}` | `{n}` | `{n}` | **{sum}** |
| 🟢 Low | `{n}` | `{n}` | — | — | **{sum}** |

---

## 7. 总体评估

| 维度 | 评级 | 说明 |
|---|---|---|
| 代码质量 | ⭐⭐⭐⭐⭐ | `{note}` |
| 功能完整性 | ⭐⭐⭐⭐⭐ | `{note}` |
| 测试覆盖 | ⭐⭐⭐⭐⭐ | `{note}` |
| **是否可以合并** | ✅/⚠️/❌ | `{verdict}` |

> 报告由 qa Agent (DeepSeek V4 Pro) 自动生成，架构师请在审查完成后核查。
